import { Component } from '@angular/core';

/**
 * Bottom footer with environment/build info (if needed).
 */
@Component({
    selector: 'scheduling-footer',
    standalone: true,
    templateUrl: './footer.component.html',
})
export class FooterComponent { }