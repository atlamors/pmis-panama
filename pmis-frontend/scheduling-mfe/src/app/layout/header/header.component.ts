import { Component } from '@angular/core';

/**
 * Top application header for the Scheduling MFE.
 * Keep minimal: branding and global actions (if any).
 */
@Component({
    selector: 'scheduling-header',
    standalone: true,
    templateUrl: './header.component.html',
})
export class HeaderComponent { }